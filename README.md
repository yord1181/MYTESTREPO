## Command Description

FindFirst is a command that combines head and grep. It searches for a word in the first few lines of a file.

To run it, use:

node findFirst.js pattern filename number

Example:

node findFirst.js error server.log 3
## AI-Assisted Programming

I asked AI to help me understand how head and grep work and how I could combine them into one command. AI helped me understand the code, come up with test cases, and figure out errors when I got stuck.

I had to choose what I wanted my command to do, run the code, and test it myself. One thing AI missed at first was that I was running my command from the wrong folder. After checking the error, I found out I needed to be in the AIworkshop folder.
